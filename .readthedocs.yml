# .readthedocs.yml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Build documentation in the docs/ directory with Sphinx
sphinx:
    configuration: docs/conf.py

# Optionally build your docs in additional formats such as PDF and ePub
formats: all

build:
    os: ubuntu-22.04
    tools:
        python: "3.11"
    jobs:
        post_create_environment:
            # Install poetry directly into the Read the Docs virtual environment
            - $READTHEDOCS_VIRTUALENV_PATH/bin/pip install "poetry>=2.0.0,<3.0.0"
            # FORCE poetry to install globally inside this specific environment context
            - $READTHEDOCS_VIRTUALENV_PATH/bin/poetry config virtualenvs.create false
        post_install:
            # Install dependencies cleanly into the RTD environment
            - $READTHEDOCS_VIRTUALENV_PATH/bin/poetry install
