name: Main

on:
    push:
        branches:
            - main
    pull_request:
        types: [opened, synchronize, reopened]

jobs:
    quality:
        runs-on: ubuntu-latest
        steps:
            - name: Check out
              uses: actions/checkout@v5

            - name: Install pnpm
              uses: pnpm/action-setup@v4
              with:
                  version: 9

            # 🛠️ Step added: Install JS dependencies so pre-commit hooks (like biome) can find their binaries
            - name: Install workspace dependencies
              run: pnpm install --frozen-lockfile

            - uses: actions/cache@v5
              with:
                  path: ~/.cache/pre-commit
                  key: pre-commit-${{ runner.os }}-${{ hashFiles('.pre-commit-config.yaml', 'packages/graphtransliterator-py/poetry.lock', 'packages/graphtransliterator-py/docs/conf.py') }}
                  restore-keys: |
                      pre-commit-${{ runner.os }}-

            - name: Set up the environment
              uses: ./.github/actions/setup-poetry-env

            - name: Run Python checks
              run: make check-py

    tox:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                python-version: ["3.11", "3.12", "3.13"]
            fail-fast: false
        steps:
            - name: Check out
              uses: actions/checkout@v5

            - name: Set up python
              uses: actions/setup-python@v6
              with:
                  python-version: ${{ matrix.python-version }}

            - name: Install Poetry
              run: pipx install poetry

            - name: Load cached venv
              uses: actions/cache@v5
              with:
                  path: packages/graphtransliterator-py/.tox
                  key: venv-${{ runner.os }}-${{ matrix.python-version }}-${{ hashFiles('packages/graphtransliterator-py/poetry.lock') }}

            - name: Install tox
              run: |
                  python -m pip install --upgrade pip
                  python -m pip install tox tox-gh-actions

            - name: Test with tox
              run: tox -c packages/graphtransliterator-py/tox.ini

            - name: Upload coverage reports to Codecov with GitHub Action on Python 3.11
              uses: codecov/codecov-action@v4
              if: ${{ matrix.python-version == '3.11' }}

    typescript-tests:
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: packages/graphtransliterator-ts
        steps:
            - name: Check out
              uses: actions/checkout@v5

            - name: Install pnpm
              uses: pnpm/action-setup@v4
              with:
                  version: 9

            - name: Set up Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: 22
                  cache: pnpm
                  cache-dependency-path: "**/pnpm-lock.yaml"

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Lint and Format Check
              run: pnpm run lint

            - name: Build TypeScript
              run: pnpm run build

            - name: Run Tests
              run: pnpm run test
